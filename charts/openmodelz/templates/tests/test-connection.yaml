apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openmodelz.fullname" . }}-test-connection"
  labels:
    {{- include "openmodelz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['agent:8080/healthz']
  restartPolicy: Never
